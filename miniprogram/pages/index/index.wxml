<!--index.wxml-->

  <view class="search-container">
            <view class="search-input">
                <i>
                    <image src="../../images/ati.png" />
                </i>
                <input type="text" placeholder-class="place-holder" placeholder="搜索" />
            </view>
            <view class="jobwant">
                <text class="jobwant-name">我要招聘</text>
            </view>
  </view>
  
  <!-- 导航 -->

  <view class="navList1">      
      
            <view wx:for="{{items}}" wx:for-item="item" wx:key="id"wx:for-items="{{items}}" data-id="{{item.name}}" value="{{item.name}}"  class='{{item.checked?"nav-item":"nav-itemno"}}' bindtap='clicks'>
                
                <text class="nav-name"  >{{item.value}}</text>
            </view>
            
          
  </view>
  <!-- 导航 -->
  <view class="navList2">   
          <navigator url="/pages/citys/citys" hover-class="navigator-hover">  
            <view class="nav-item">
                <image class="nav-icon" src="{{item.image}}" />
                <text class="nav-name">{{city}}</text>
            </view>
          </navigator> 
            <view class="nav-item">
                <image class="nav-icon" src="{{item.image}}" />
                <text class="nav-name">职位</text>
            </view>
            <view class="nav-item">
                <image class="nav-icon" src="{{item.image}}" />
                <text class="nav-name">公司</text>
            </view>
            <view class="nav-item">
                <image class="nav-icon" src="{{item.image}}" />
                <text class="nav-name">薪资</text>
            </view>
  </view>
  
 
<!--滚动tab-->

    <scroll-view scroll-y="true" style="height: 930rpx;" class="scroll" >
          <view wx:for="{{demo}}" id="demo1" class="scroll-view-item "></view>
         
    </scroll-view>

      
           

      
